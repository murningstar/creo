# UX Requirements

## Visual Feedback

- **Начало записи:** пульс-волна (circle) расширяющаяся от индикатора на весь экран — привлекает периферийное зрение
- **Во время диктовки:** компактный circular waveform индикатор (не широкий прямоугольник)
- **Idle:** еле заметный индикатор (как в OpenWhispr)

## Overlay Indicator (отдельное окно)

- Отдельное Tauri окно: AlwaysOnTop, fullscreen, полностью прозрачный фон
- Вся область click-through (прокликивается насквозь к приложениям под ним)
- Пульс/волна при старте записи видна периферийным зрением, не мешает работе
- Tauri window options: `transparent`, `decorations: false`, `always_on_top`, `ignore_cursor_events`

## Text Input

- Два метода: **Paste** (default) и **Type** (enigo)
- Paste: clipboard → Ctrl+V/Cmd+V/Ctrl+Shift+V, без restore (перезаписывает clipboard)
- Type: enigo посимвольно (может лагать в тяжёлых редакторах)
- Настройка в settings: segmented control Paste / Type
- Info banner при выборе каждого метода объясняет нюансы

## Hotkey

- Default: зажатие (push-to-talk), не toggle
- Комбинация по умолчанию: `Ctrl+`` (backtick), определена по cross-platform research (.claude/docs/hotkeys/)
- Полностью конфигурируемая: record shortcut UI в settings
- Режим: segmented control Нажатие (toggle) / Зажатие (push-to-talk)
- НЕ показывается в wizard — сообщается модально после завершения wizard

## Wake Word (embedding + DTW)

- Пользователь записывает 3-5 сэмплов каждой команды
- Кастомное имя ассистента (default: "Крео", меняется в wizard)
- Тренировка команд в wizard: сначала "приём", потом остальные
- Language-agnostic: работает на уровне звука, не текста

## History

- Настраиваемый retention (дни)
- Отображается на Main экране
- Может быть перемещена в отдельный виджет/страницу позже

## Wizard (первый запуск)

1. Определение системы (GPU/CPU/RAM) → opinionated выбор модели
2. Скачивание моделей (или инструкция + polling)
3. Имя ассистента (input, default "Крео")
4. Тренировка команды "приём" (3-5 записей)
5. Показ overlay + бот оповещает о необходимости создать другие команды
6. Тренировка остальных команд
7. Готово: модальное уведомление о hotkey, сворачивается в tray

## Banners / Guides (полный реестр)

### Wizard / First Launch

| Баннер               | Платформа | Когда        | Содержание                                                                                            |
| -------------------- | --------- | ------------ | ----------------------------------------------------------------------------------------------------- |
| GPU detected         | All       | Wizard шаг 1 | Позитив: "Отлично, обнаружено аппаратное ускорение (GPU). Качество распознавания будет максимальным." |
| CPU only             | All       | Wizard шаг 1 | Info: "GPU не обнаружен. Подобрана оптимальная модель для вашего процессора."                         |
| Model recommendation | All       | Wizard шаг 1 | Почему выбрана эта модель, что можно изменить в настройках                                            |
| Hotkey info          | All       | Wizard финал | Модально: "Горячая клавиша по умолчанию: X. Режим: зажатие. Можно изменить в настройках."             |

### Runtime (при каждом запуске, условно)

| Баннер                         | Платформа     | Когда                     | Содержание                                                                                                                                                                                                          |
| ------------------------------ | ------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Models required                | All           | Модели не найдены         | "Поместите файлы моделей в: [путь]" — **реализовано**                                                                                                                                                               |
| Admin elevation                | Windows       | Не admin                  | "Приложение запущено без прав администратора. Горячая клавиша и ввод текста не будут работать в окнах с повышенными правами (терминалы, IDE)."                                                                      |
| Cyrillic path                  | Windows       | Non-ASCII user path       | "Обнаружен нестандартный путь пользователя. Модели хранятся в [ASCII путь] для совместимости."                                                                                                                      |
| Wayland relogin                | Linux Wayland | Группа `input` не активна | "Для работы горячих клавиш на Wayland необходим перелогин. Пока используйте голосовые команды."                                                                                                                     |
| macOS Accessibility pre-prompt | macOS         | Перед запросом permission | "Сейчас система запросит разрешение на управление клавиатурой. Это необходимо для: (1) горячей клавиши из любого окна, (2) ввода надиктованного текста в приложения. Без этого разрешения Creo не сможет работать." |
| macOS Microphone pre-prompt    | macOS         | Перед запросом permission | "Сейчас система запросит доступ к микрофону. Это необходимо для распознавания голосовых команд и диктовки."                                                                                                         |

### Settings

| Баннер                 | Платформа | Когда               | Содержание                                                                                                                  |
| ---------------------- | --------- | ------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Input method: Paste    | All       | Выбран Paste        | "Надиктованный текст заменяет содержимое буфера обмена."                                                                    |
| Input method: Type     | All       | Выбран Type         | "Может быть медленным для длинных текстов — каждый символ вызывает перерисовку в приложении (подсветка синтаксиса и т.д.)." |
| Hotkey platform limits | Wayland   | В настройках hotkey | "На Wayland требуется членство в группе `input`. [Инструкция]"                                                              |
| Hotkey single-key tip  | All       | В настройках hotkey | "Если на клавиатуре есть ScrollLock/Pause — советуем поставить на них. Одной кнопкой вызывать удобнее."                     |

### Wizard / Tour (экран обучения команд)

| Баннер         | Платформа | Когда                                       | Содержание                                                                                                                                                                              |
| -------------- | --------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Hotkey overlay | All       | На экране тренировки команд, в углу overlay | Показывает текущий шорткат для диктовки: "Ctrl+\` — зажмите для диктовки". Также баннер: "Если у вас есть ScrollLock — советуем поставить на него в настройках, одной кнопкой удобнее." |
